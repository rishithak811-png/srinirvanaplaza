PORT=5001

# MySQL Configuration (If using MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=room_service

# To use SQLite fallback automatically, leave MySQL config empty or let it fail connection.
# The server will automatically create and use database.sqlite.
